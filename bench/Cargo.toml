[package]
name = "roc-net-bench"
version = "0.0.0"
edition = "2021"
publish = false

# Standalone: not part of the host crate's build.
[workspace]

[dependencies]
libc = "0.2"
tokio = { version = "1", features = ["rt-multi-thread", "net", "io-util", "macros"] }

[profile.release]
debug = false
